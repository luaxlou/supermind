# 迭代能力内容审查

审查对象为 `iteration.md`。能力库完整检索没有覆盖“调用 Superpowers 完成实施并管理软件版本”的合格能力，故新增独立工程方法，分类为“迭代”。只建立能力，不对当前项目启动一次软件实施，也不修改 Supermind Skill。

依赖核实：读取现有 Superpowers 的执行计划、计划编写、子代理实施、完成验证及分支收尾说明，并核实官方仓库及安装入口。正文引用实际技能，不复制其详细执行流程；具体调用需读取当时技能全文。版本和迭代交接规则是本方法的补充，不声称是 Superpowers 自动提供的功能。

## 独立迁移推演

虚构预约服务基线要求“取消后释放名额”，对齐状态为实现待对齐，设计已确认；用户授权实施，项目软件版本为 2.3.1，现行规则认定兼容缺陷修复递增修订版本。方法产出一轮迭代：范围为取消与名额一致性，目标软件版本 2.3.2，使用现有计划或 Superpowers 计划及执行技能，补齐约定验证与审查；版本记录连接实际提交与变更说明，对齐状态按证据关闭。基线仍为原版本，不生成快照。未授权发布时标实施已完成、未发布；这里是拟产出的记录，不伪造实际代码、提交或测试通过。

## 改变假设

无正式基线的命令行工具收到编码错误报告，采用方使用日期版本并授权修复。来源记录真实缺陷，不补造基线；按其日期规则安排版本，计划规模缩小，Superpowers 按缺口调用。若两个组件独立发布，分别维护受影响版本，其他组件不变。若多个迭代进入同一未发布版本，只更新待发布记录和本轮提交映射，不每轮强制打新标签。

## 拒绝与缺口检查

- 仅建立基线且明确暂缓开发：不启动实施，记录待开始及恢复条件。
- 关键 Superpowers 技能缺失：报告依赖缺口，不把自写执行流程说成已经使用 Superpowers。
- 实现已写完但人工验收未完成：交接待验收，不标全部已对齐或迭代已完成。
- 已发布版本发现遗漏：使用新版本修正，不移动旧标签；未授权发布不把版本记录等同于发布动作。
- 软件版本上涨但需求未重大变化：不为此归档基线。迭代范围调整有决定与去向，不能静默丢弃未完成任务。

正文不依赖原业务、私有会话、本机路径或隐含编号规则。必要差异均由采用方输入。推演证明的是方法可理解且有明确输出，不是真实运行或复用成功证据。

## 登记依据

正文 SHA-256：`b4b4e3d5b7ce84e3b594bde7f97bad0a7ca16c15c0d3984cb1b7da1895fd6610`。入库参数以相对单位估计：预计采用次数 3、单次收益 1、提取 0.5、接入 0.4、内容审查 0.3、维护 0.3、风险 0.2，净值 1.3。仅是事前估计，不是实际 ROI 或工时。登记内容审查证据，保持候选与抽象中，不伪造真实采用或运行验证。

生成卡检查发现段落内依赖链接被转义，已改为生成器支持的独立链接，便于直接获取官方项目及安装说明。仅链接呈现调整，当前正文摘要为 `76170265eb70a6f877f5da8f7266af310d124ca29368a36bbdcca17ba8b56900`，替代此前摘要，采用方法与内容审查结论不变。


## 2026-09-08 供应方依赖修订（当前有效审查）

上文为旧版审查历史，曾将指定插件固化为实施前提，与用户禁用决定冲突；其依赖核实和推演不再支撑当前采用。当前正文以项目工程约束为执行依据，独立表达目标、范围、计划、版本、实现、原因定位、验证及交接；不复制或换名转述插件流程。

独立迁移一：虚构预约服务修复“取消后未释放名额”，范围获准，软件 2.3.1，项目允许单一端到端检查并禁用第三方执行插件。输出仍为已有迭代记录中的一项修复，按项目规则安排 2.3.2；直接定位取消与名额的一致性，修改后执行允许的检查，记录实际修订和结果。没有插件不构成阻断，也不引入 TDD、Mock、多代理或额外账本；未授权发布则保留未发布状态。这是方法输出推演，未实际修改该虚构服务。

独立迁移二：无正式基线的命令行编码缺陷，日期版本，用户要求只提交修复供人工验收。缺陷来源、修改范围和实际开发修订进入现有记录，按日期版本规则维护待发布说明；不补造基线，不运行未授权验证，交接待验收而不是已验证。

变化与拒绝：项目允许某个辅助工具且确有实际缺口时可以按授权选用，但不会改变独立方法；若用户禁用该工具或其供应方，选择范围直接排除。真实业务运行依赖不可用则报告具体阻断，不把可选插件的缺失当成业务依赖。仅讨论、暂缓开发或未授权发布的边界继续成立。

审查结论：只凭修订正文和采用方输入即可形成独立实施/交接判断；不需旧插件资料、原项目或会话。当前验证为内容审查与虚构情境推演，不冒充实际采用或量化收益。旧摘要、约束和模板字段必须随正文一起更新；库内抽象状态另行判断，不因移除名称而升级。

当前正文 SHA-256：a2796c1e0d5660848b49a05e405832bba04f0833ea6093417e307e427358db19。
